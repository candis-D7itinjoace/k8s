# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  push:
    branches:
      - "master"
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  deploy:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - uses: actions/checkout@v3
      - name: login to docker
        
        run: |
          docker login -u ${{secrets.DOCKER_USER}} -p ${{secrets.DOCKER_PASSWORD}}
      
      - name: Azure Login
        uses: Azure/login@v1.4.6
        with: 
          creds: ${{secrets.AKs_CREDS}}
      - name: AKS set context
        uses: azure/aks-set-context@v3
        with:
          resource-group: 'asserter-resource-group'
          cluster-name: 'assert-please'

      - name: setup kubectl
        uses: Azure/setup-kubectl@v3
      
      - name: Deploy mysql to kube AKS
        uses: Azure/k8s-deploy@v4
        with:
          manifests: './database.yml'
      - name: deploy domain-header-app to AKS
        uses: Azure/k8s-deploy@v4
        with:
          manifests: './domain-header.yml'
      
      - name: announce the end

        run: echo "hello end" 
        


          
